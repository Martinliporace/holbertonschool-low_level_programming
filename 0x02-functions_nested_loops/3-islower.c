#include "main.h"

/**
* _islower - Checks for lowercase character
*
*
* Description: Show a message blablabla
* Return: Always 0 (Success)
**/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
